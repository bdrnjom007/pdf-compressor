# 📖 دليل نشر موقع ضغط PDF على GitHub Pages

## ✨ ما تم إنجازه:

✅ تم إنشاء هيكل المشروع
✅ تم إعداد git repository
✅ تم تعديل الواجهة للاتصال بالخادم البعيد
✅ تم إنشاء مجلد `docs/` لـ GitHub Pages

---

## 📝 الخطوات المتبقية:

### الخطوة 1: إنشاء Repository على GitHub

1. اذهب إلى: https://github.com/new
2. اسم الريبو: `pdf-compressor`
3. اختر **Public**
4. ❌ لا تفعّل "Add a README file"
5. اضغط **Create repository**

### الخطوة 2: رفع الكود على GitHub

افتح Terminal ونفذ هذه الأوامر:

```bash
# الانتقال لمجلد المشروع
cd /Users/bdalhkymalhbysh/pdf-compressor

# استبدل YOUR_USERNAME باسمك في GitHub
git remote add origin https://github.com/YOUR_USERNAME/pdf-compressor.git

# تغيير اسم الفرع إلى main
git branch -M main

# رفع الكود
git push -u origin main
```

### الخطوة 3: تفعيل GitHub Pages

1. افتح الريبو على GitHub
2. اضغط على **Settings** (الإعدادات)
3. من القائمة الجانبية اختر **Pages**
4. تحت **Source**:
   - اختر: **Deploy from a branch**
   - Branch: `main`
   - Folder: `/docs`
5. اضغط **Save**

### الخطوة 4: انتظر النشر

سيظهر رابط الموقع بعد دقائق:
```
https://YOUR_USERNAME.github.io/pdf-compressor/
```

---

## 🧪 اختبار الموقع:

1. افتح الرابط الجديد
2. ارفع ملف PDF
3. اختر مستوى الضغط
4. اضغط "ضغط الملف"

الموقع سيتصل تلقائياً بالخادم: `http://82.25.116.131:8080`

---

## ⚙️ كيف يعمل:

```
📱 GitHub Pages (الواجهة)
        ↓
    HTTP Request
        ↓
🖥️ الخادم البعيد (82.25.116.131:8080)
        ↓
    معالجة + ضغط PDF
        ↓
📦 رفع الملف المضغوط
```

---

## 🔧 تعديل رابط الخادم:

إذا أردت تغيير رابط الخادم:

1. افتح: `docs/js/app.js`
2. عدل السطر 9:
```javascript
: 'http://82.25.116.131:8080';  // غيّر هذا الرابط
```
3. أعد رفع التعديلات:
```bash
git add .
git commit -m "Update API URL"
git push
```

---

## 📂 هيكل المشروع:

```
pdf-compressor/
├── docs/              ← ملفات GitHub Pages
│   ├── index.html
│   ├── css/
│   └── js/
├── public/            ← نسخة محلية
├── server.js          ← الخادم (Backend)
├── package.json
└── README.md
```

---

## ✅ بعد النشر:

- ✅ الواجهة تعمل على GitHub Pages مجاناً
- ✅ الخادم يعمل على 82.25.116.131:8080
- ✅ يمكن مشاركة الرابط مع أي شخص
- ✅ لا حاجة لاستضافة أو تكلفة شهرية

---

## 🔐 ملاحظة أمنية:

الخادم الحالي يستخدم HTTP (غير مشفّر). للتطبيق الإنتاجي يُنصح بـ:
- تفعيل HTTPS على الخادم
- إضافة CORS headers
- إضافة rate limiting

---

تم إعداد هذا الملف بواسطة Claude Code
التاريخ: 2026-02-09
